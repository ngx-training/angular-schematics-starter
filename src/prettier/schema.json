{
  "$schema": "http://json-schema.org/schema",
  "id": "PrettierSchematic",
  "title": "Generate prettier file",
  "type": "object",
  "description": "Creates a new prettier file",
  "properties": {
    "path": {
      "type": "string",
      "description": "Path for prettier generation",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "Where should be prettier file generated?"
    }
  }
}
